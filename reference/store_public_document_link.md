# Store Public Document Link

Store the GCS link to a document in the database

## Usage

``` r
store_public_document_link(db, id)
```

## Arguments

- db:

  A database connection pool created with
  [`pool::dbPool`](http://rstudio.github.io/pool/reference/dbPool.md)

- id:

  The id of the document
