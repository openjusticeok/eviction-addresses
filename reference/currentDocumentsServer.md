# Current Documents Module Server

This function creates the server for the current documents module.

## Usage

``` r
currentDocumentsServer(id, current_case, db)
```

## Arguments

- id:

  The id of the module

- current_case:

  The reactive value containing the current case

- db:

  The database connection

## Value

The server function for the current documents module
