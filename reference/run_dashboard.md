# Run Dashboard

Runs the eviction address entry dashboard

## Usage

``` r
run_dashboard(config, ...)
```

## Arguments

- config:

  The path to a config.yml file

- ...:

  Additional arguments passed to
  [`shiny::shinyApp`](https://rdrr.io/pkg/shiny/man/shinyApp.html)
