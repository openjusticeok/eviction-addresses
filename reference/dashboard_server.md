# Eviction Addresses Dashboard Server

Generates the server function passed to `shinyApp`

## Usage

``` r
dashboard_server(config)
```

## Arguments

- config:

  The path to a config file

## Value

A Shiny server function
