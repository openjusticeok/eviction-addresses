# Has Names

Has Names

## Usage

``` r
has_names(x, names)
```

## Arguments

- x:

  The object to check for names

- names:

  A character vector of names to check `x` for

## Value

A boolean (logical vector of length 1)
